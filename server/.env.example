PORT=5000
JWT_SECRET=replace-with-a-random-secret
DATABASE_URL="file:./prisma/dev.db"
HOLD_TTL_MINUTES=10
WAITLIST_OFFER_MINUTES=30

# Optional — omit to log emails to the console instead of sending them
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=
